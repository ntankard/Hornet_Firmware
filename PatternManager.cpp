#pragma once 

#include "PatternManager.h"

PatternManager::PatternManager()
{
	_startPattern = new Pattern(patternNULL);
	_endPattern = new Pattern(patternNULL);

void PatternManager::addPattern(Point* startPoint, Point* endPoint)
{

}